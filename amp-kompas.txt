<!doctype html>
<html amp lang="id">
<head>
 <meta charset="utf-8">
 <title>{{title}}</title>
 <meta name="viewport" content="width=device-width, initial-scale=1">
 <meta name="description" content="{{description}}">
 <meta name="robots" content="index,follow">
 <link rel="canonical" href="{{canonical}}">
 <meta property="og:url" content="{{canonical}}">
 <meta property="og:title" content="{{title}}">
 <meta property="og:description" content="{{description}}">
 <meta property="og:site_name" content="{{brand_name}}">
 <meta property="og:image" content="{{banner}}">
 <meta property="og:image:alt" content="{{title}}.">
 <link rel="icon" type="image/png" href="{{favicon}}">
 <script async src="https://cdn.ampproject.org/v0.js"></script>
 <script async custom-element="amp-carousel" src="https://cdn.ampproject.org/v0/amp-carousel-0.1.js"></script>
 <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
 <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
 <style amp-boilerplate>body{-webkit-animation:-amp-start 8s steps(1,end) 0s 1 normal both;-moz-animation:-amp-start 8s steps(1,end) 0s 1 normal both;-ms-animation:-amp-start 8s steps(1,end) 0s 1 normal both;animation:-amp-start 8s steps(1,end) 0s 1 normal both}@-webkit-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@-moz-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@-ms-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@-o-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}</style>
 <noscript>
  <style amp-boilerplate>body{-webkit-animation:none;-moz-animation:none;-ms-animation:none;animation:none}</style>
 </noscript>
 <style amp-custom>

/* --- PALET WARNA: NEON PURPLE VOID --- */
:root{
  --bg: #0A0700;          /* Very Dark Gold/Black */
  --bg-soft: #1A1300;     /* Soft Dark Brown-Gold */
  --card: #610049;        /* Deep Gold-Brown */
  --text: #FFFFFF;        /* White for max contrast */
  --muted: #f7f6f3;       /* Soft Pastel Gold */
  --neon-main: #e539fcb0;   /* Bright Gold Neon */
  --neon-accent: #f700ff; /* Pure Gold Accent */
  --ring: rgba(243, 242, 238, 0.45); /* Gold Glow */
}


  *{box-sizing:border-box}
  html,body{margin:0}
  body{
   font-family:'Inter',system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,'Helvetica Neue',Arial,'Noto Sans',sans-serif;
   color:var(--text);
 
background:
  radial-gradient(
    1000px 500px at 20% -10%,
    var(--neon-main) 0%,
    transparent 60%
  ), /* Neon Main Glow */

  radial-gradient(
    800px 400px at 120% 10%,
    var(--neon-accent) 0%,
    transparent 60%
  ), /* Neon Accent Glow */

  var(--bg);

min-height: 100vh;
   
   text-shadow: 0 0 2px rgba(186, 85, 211, 0.1); /* Pinkish Text Shadow */
  }
  .shell{
   max-width:520px;margin:0 auto;padding:20px 16px 36px;
   display:flex;flex-direction:column;gap:16px;
  }

  .header{display:flex;align-items:center;justify-content:center;gap:12px;text-align:center}
  .logo{border-radius:8px}
  .title{
   margin:0;font-size:20px;font-weight:700;
   color: var(--neon-accent);
   text-shadow: 0 0 10px var(--neon-main);
  }
  .subtitle{margin:2px 0 0;font-size:12px;color:var(--muted)}

  .card{
   background:var(--card);
   border:1px solid rgba(190, 85, 211, 0.2); /* Pink Border */
   border-radius:14px; 
   box-shadow: 0 0 15px rgba(211, 85, 184, 0.25), 0 5px 20px rgba(56, 3, 61, 0.5); /* Pink Shadow */
   overflow:hidden;
   transition: all 0.3s ease;
  }
  .hero-card {
    padding:12px 12px 0 12px; 
    border:1px solid var(--neon-main); 
    box-shadow: 0 0 25px var(--neon-main), 0 0 50px rgba(196, 85, 211, 0.4); 
    animation: pulse-glow 2s infinite alternate ease-in-out; 
  }

  .hero-card:hover {
    box-shadow: 0 0 35px var(--neon-accent), 0 0 70px rgba(211, 177, 85, 0.6); 
    transform: translateY(-2px);
  }

  @keyframes pulse-glow {
    from { box-shadow: 0 0 20px var(--neon-main), 0 0 40px rgba(211, 85, 173, 0.3); }
    to { box-shadow: 0 0 30px var(--neon-accent), 0 0 60px rgba(211, 85, 205, 0.5); }
  }

  .hero-img{border-radius:10px} 
  
  @keyframes pulse-image-light {
    0% {
        transform: scale(0.7); 
        opacity: 0;
        box-shadow: 0 0 15px var(--neon-main); 
    }
    40% { 
        transform: scale(1.08); 
        opacity: 0.9; 
        box-shadow: 0 0 30px var(--neon-accent), 0 0 60px rgba(211, 171, 85, 0.4); 
    }
    100% {
        transform: scale(1.3); 
        opacity: 0;
        box-shadow: 0 0 50px var(--neon-main); 
    }
  }

  .img-wrap { 
    position: relative; 
    border-radius: 12px; 
    overflow: hidden; 
    transition: all 0.5s ease;
    box-shadow: 0 0 15px var(--neon-main), 
                0 0 30px rgba(186, 85, 211, 0.2);
    z-index: 1; 
  }

  .img-wrap::after {
    content: '';
    position: absolute;
    top: -8px; 
    left: -8px;
    right: -8px;
    bottom: -8px;
    border-radius: 18px; 
    background: transparent;
    border: 4px solid var(--neon-accent); 
    opacity: 0;
    transform: scale(0.7); 
    animation: pulse-image-light 3s infinite alternate ease-in-out; 
    z-index: 0; 
  }
  
  .img-wrap:hover {
    transform: scale(1.02); 
    box-shadow: 0 0 25px var(--neon-accent),
                0 0 50px rgba(211, 171, 85, 0.4); 
  }

  @keyframes bg-pan{from{background-position:0% 50%}to{background-position:100% 50%}}

  .cta{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:12px; padding: 0 12px 12px 12px;}
  .btn{
   display:inline-block;text-decoration:none;text-align:center;
   padding:14px 16px;border-radius:10px;font-weight:700;letter-spacing:.5px;
   border:1px solid transparent;transition:background .25s ease,color .25s ease,box-shadow .25s ease,transform .12s ease;
   will-change:background,transform;
   box-shadow: 0 0 10px rgba(186, 85, 211, 0.3); 
   transform: skewX(-5deg); 
  }
  .btn:nth-child(2){
    transform: skewX(5deg); 
  }

  .btn-primary{
   background:linear-gradient(135deg, var(--neon-main), var(--neon-accent));
   color: #000000; /* Teks Hitam kontras dengan pink */
   border-color: rgba(255, 255, 255, .1);
   text-shadow: none;
  }
  .btn-secondary{
   background: var(--bg-soft);
   color: var(--neon-main);
   border-color: var(--neon-main);
   text-shadow: 0 0 5px var(--neon-main);
   background-image:linear-gradient(90deg, var(--bg-soft), var(--bg-soft)); /* Pinkish gradient */
   background-size:200% 100%;
  }

  .btn-primary:hover{
   background: linear-gradient(90deg, #c5bdb1, #ff00d4); /* Lebih cerah saat hover */
   transform:translateY(-2px) skewX(-5deg);
   box-shadow: 0 0 20px var(--neon-accent), 0 5px 15px rgba(0, 0, 0, 0.5); 
  }
  .btn-secondary:hover{
   animation:bg-pan 1.4s ease-in-out infinite alternate;
   transform:translateY(-2px) skewX(5deg);
   border-color: var(--neon-accent);
   box-shadow: 0 0 20px var(--neon-main), 0 5px 15px rgba(0, 0, 0, 0.5); 
  }
  .btn:focus-visible{outline:0;box-shadow:0 0 0 6px var(--ring)}
  .btn:active{transform:translateY(0)}

  
  .table-container {
   padding: 10px 15px 0;
  }

  .table-container table {
   width: 100%;
   border-collapse: collapse;
   margin: 10px 0;
   font-size: 13px;
   box-shadow: 0 0 10px rgba(211, 171, 85, 0.1);
   border-radius: 8px; 
   overflow: hidden; 
  }

  .table-container table tr:nth-child(odd) {
   background-color: var(--bg-soft);
  }

  .table-container table tr:nth-child(even) {
   background-color: var(--card);
  }

  .table-container table tr:last-child td {
   border-bottom: none;
  }

  .table-container table td {
   padding: 8px 12px;
   border-bottom: 1px solid rgba(211, 156, 85, 0.1);
   line-height: 1.4;
  }

  .table-container table td:first-child {
   font-weight: 600;
   color: var(--neon-accent);
   width: 50%;
   text-shadow: 0 0 5px rgba(211, 167, 85, 0.3);
  }

  .table-container table td:last-child {
   text-align: right;
   font-weight: 400;
   color: var(--text);
  }
  
  /* --- STYLE BARU UNTUK RTP --- */
  .rtp-feature {
    padding: 16px;
  }
  .rtp-feature h2 {
    margin: 0 0 16px;
    font-size: 16px;
    font-weight: 700;
    color: var(--neon-accent);
    text-shadow: 0 0 8px var(--neon-main);
    text-align: center;
  }
  .rtp-list {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .rtp-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 10px 15px;
    background: var(--bg-soft);
    border-radius: 8px;
    border: 1px solid var(--neon-main);
    box-shadow: 0 0 10px rgba(211, 167, 85, 0.2);
  }
  .rtp-game-name {
    font-weight: 600;
    font-size: 14px;
    color: var(--text);
  }
  .rtp-meter {
    flex-grow: 1;
    height: 12px;
    background: var(--card);
    border-radius: 6px;
    overflow: hidden;
    margin: 0 15px;
  }
  .rtp-bar {
    height: 100%;
    background: linear-gradient(90deg, #f0647c, #ff0000);
    border-radius: 6px;
    transition: width 1s ease-in-out;
  }
  .rtp-percentage {
    font-weight: 700;
    color: var(--neon-main);
    text-shadow: 0 0 5px var(--neon-accent);
    width: 45px;
    text-align: right;
  }
  /* =================================== */

  .footer{text-align:center;color:var(--muted);font-size:12px;margin-top:8px}
  .footer b{color:var(--neon-main); text-shadow: 0 0 5px var(--neon-main);}

  @media (prefers-reduced-motion: reduce){
   .btn:hover{transform:none}
   .hero-card{animation:none;}
   .img-wrap::after{animation:none;} 
  }
 </style>
</head>
<body>
 <div class="shell">
  <header class="header">
   <amp-img
    class="logo"
    src="{{logo}}"
    width="200" height="70" layout="fixed"
    alt="{{brand_name}} - Logo">
   </amp-img>
  </header>
  
  <section class="card hero-card">
   <div class="img-wrap">
   <amp-img
    class="hero-img"
    src="{{banner}}"
    width="1946" height="1946" layout="responsive"
    alt="{{brand_name}}">
   </amp-img>
   </div>

<div class="table-container">
  <table>
    <tbody>
      <tr>
        <td>⚡ Hanya Perlu</td>
        <td>Rp 10.000</td>
      </tr>
      <tr>
        <td>💳 Winrate</td>
        <td>98.06%</td>
      </tr>
      <tr>
        <td>🕒 Jam Dukungan</td>
        <td>24 jam</td>
      </tr>
      <tr>
        <td>🔗 Metode Transaksi</td>
        <td>QRIS, Virtual Account, Wallet</td>
      </tr>
    </tbody>
  </table>
</div>

   <div class="cta">
    <a href="{{moneysite-link}}" target="_blank" rel="noreferrer noopener" class="btn btn-primary">LOGIN</a>
    <a href="{{moneysite-link}}" target="_blank" rel="noreferrer noopener" class="btn btn-secondary">DAFTAR</a>
   </div>
   
  </section>

<section class="card rtp-feature">
  <h2>🔥 REKOMENDASI GAME POPULER HARI INI 🔥</h2>
  <div class="rtp-list">

    <div class="rtp-item">
      <div class="rtp-game-name">Gates of olympus</div>
      <div class="rtp-meter">
        <div class="rtp-bar" style="width: 92.7%;"></div>
      </div>
      <div class="rtp-percentage">92.7%</div>
    </div>

    <div class="rtp-item">
      <div class="rtp-game-name">Mahjong Wins 2</div>
      <div class="rtp-meter">
        <div class="rtp-bar" style="width: 89.4%;"></div>
      </div>
      <div class="rtp-percentage">89.4%</div>
    </div>

    <div class="rtp-item">
      <div class="rtp-game-name">Golden Wild</div>
      <div class="rtp-meter">
        <div class="rtp-bar" style="width: 93.8%;"></div>
      </div>
      <div class="rtp-percentage">93.8%</div>
    </div>

    <div class="rtp-item">
      <div class="rtp-game-name">Lucky Neko</div>
      <div class="rtp-meter">
        <div class="rtp-bar" style="width: 91.2%;"></div>
      </div>
      <div class="rtp-percentage">91.2%</div>
    </div>

    <div class="rtp-item">
      <div class="rtp-game-name">Starlight Princess</div>
      <div class="rtp-meter">
        <div class="rtp-bar" style="width: 94.5%;"></div>
      </div>
      <div class="rtp-percentage">94.5%</div>
    </div>

  </div>
</section>
   </div>
  </section>
  <footer class="footer">
   © 2026 <b>{{brand_name}}</b> • S3OM4G4NG
  </footer>
 </div>
</body>
</html>
